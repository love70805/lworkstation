# Lworkstation 0.2.10 更新验收

状态：实施中，尚未形成候选安装包，尚未发布。当前公开稳定版 0.2.9。

## 已批准范围

确定待查 SKC 后自动关联回传；分批查询与草稿独立接收；扩展复制/CSV 导入兼容；明确传递状态与异常处理；人工更正仅成本和说明、优先且可撤销；精确累计及定稿保护；经营首页整合利润、账本/成本侧栏独立；紧凑版本弹窗；首次安装向导选择路径，升级保留路径和数据。

详细业务约束、模块边界、代码基线与验收标准见 [更新合同](integration/UPDATE_0.2.10.md)。

## 验收记录

- 更新前工作区干净，集成基线 `3d364acddeb1a3894a0950ef2ffa48216080929c`，远端一致。
- 2026-09-10 前端与桌面依赖审计均未发现已知漏洞。
- 模块交付、集成回归、安装包/路径/升级验收、GitHub PR/CI、公开资产回读待完成；不据任务已派发声称完成。
- 不执行线上 SQL；本轮真实 ERP 账号现场状态尚未验证，不修改真实业务数据。

## 桌面模块阶段验收（不是最终安装包验收）

- 专职 `6fc33a1` → 主线 `c524d08`：assisted NSIS、路径保留钩子、紧凑更新弹窗。主线 11 组 desktop verify 通过。
- 主线视觉复核发现短文案弹窗有多余纵向滚动条，退回专职修订 `86c5331` → 主线 `a1b04dd`，测量加入边框。主线真实 Electron 渲染夹具 18 种明暗/更新状态通过，短状态无横纵溢出，展开说明可滚动、操作可达、收起恢复高度。
- 专职真实 NSIS 使用独立 `com.shopeers.installer-smoke.0210` 合成载荷：观察到首装用户/路径页；通过受支持的 `/S /D` 验证中文空格路径安装；随后 `--updated` 不带 `/S` 或 `/D` 完成升级，安装目录、快捷方式目标及 AppData 哨兵保持。原生截图/路径输入 API 不可用，不能将该结果称为已通过正式软件的可见路径手动输入验收。
- 18 个合成安装夹具/证据文件已归档并逐文件核对 SHA-256，位置 `archive/release-0.2.10-2026-09-10/desktop-installer/`。主线弹窗图片与结果在同目录的 `popover-reviewed/`；初次待修截图独立保留于 `popover-initial/`。

## 业务与首页阶段验收

- 人工成本 `dc5b71a` → `a5ab985`；ERP 请求/收件与扩展 8.0.17 `33feacf` → `0f93ba5`；首页组合 `7d5833f` → `cc11371`。原 8.0.16 下载资源保留，新 8.0.17 ZIP SHA-256：`686086F15CE85D0682273E23354D5D59A935342783C8018DC9EDA5B318738316`。
- 主线在人工/ERP 初次合并状态完整 `frontend release:check` 通过；首页合并后 `desktop verify` 11 组通过。后续修订完成仍需最终完整回归。
- 主线通过真实浏览器界面导入两个合成店铺的同月 CSV，验证同一 SKU 跨店隔离、人工 `0.0000001 × 100000 = 0.01`、真实零成本、保存后刷新、撤销无 ERP 时回到待补。另店单价 2 保持不变；全部店铺采购成本 20.01、仓储 70008.40、利润 130011.59，与独立预期相符。未访问真实安装数据。
- 首页/月度账本/成本侧栏保留已验证月份和店铺，旧 `/profit` 兼容重定向。专职真实 Electron 三尺寸 1024×768、1280×800、390×780 单一页面壳、无页面横向溢出、按钮和弹窗可达；主线复核图片及独立浏览器导航。7 张图片与结果哈希归档于 `workspace-layout/`。
- 审查发现仓储弹窗嵌在默认折叠的明细中；修订 `6406d45` → `1998696` 移至折叠容器外，并添加真实组件点击测试；主线浏览器确认默认折叠时弹窗可打开、可关闭。
- 受控重开 `b3b62f9` → `1325a10` 已通过独立复审及 3 文件 66 项定向测试。主线真实浏览器验证：甲店筛选下定稿仍覆盖全部店铺；刷新后保持原 130011.59 快照、修改入口禁用；明确填写原因重开后，将乙店成本从 2 改为 1.5，再次定稿并刷新保持采购 15.01、利润 130016.59。审计原快照和混合 ERP 不作废由 repository/sync 回归验证。
- 离线请求修订 `6103d7f` → `e021c4c` 通过独立复审；同账本旧登记在服务端串行替换/取消，跨账本范围和歧义拒绝保留。最终完整 `frontend release:check` 通过：86 文件、581 测试、2114 模块生产构建、桥接、ERP 协议/并发落盘、同步冒烟、种子与现有 PostgreSQL Schema、部署门禁。日志 `integration-check-final.log`；生产构建仍有既有大 chunk 提示。
- 当前剩余门槛：SQL 0012 兼容迁移文件与测试待用户单独批准，尚未写入；现有 Schema 门禁通过不代表该兼容迁移已完成。本轮不执行线上数据库变更。受保护分支合并、正式候选构建及 packaged/update 验收、公开发布均待后续完成。
