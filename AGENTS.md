# Shopeers 项目开发规则

## 项目定位

Shopeers 是组内使用的中文经营管理工作台，整合选品商品档案、1688 参考采集、ERP 成本回传和月度利润核算。

## 不可改变的业务口径

- 默认币种为人民币（CNY）。
- 平台 SKC 是商品父级标识；平台 SKU 是工作区全局唯一标识，也是 SKC 下的属性分支。
- 选品工作台负责商品档案、供应商、1688 链接、图片、售价、销售状态和参考成本/利润。
- 利润面板负责月度精确核算：台账收入、ERP 正式成本、仓储成本和最终利润。
- ERP 成本优先；1688 成本只作参考。
- ERP 成本缺失时，人工确认成本必须有明确来源和状态，不能静默覆盖已定稿月度利润。

## 多任务边界

- 选品工作台：`ProductLibrary`、`ProductEditor`、`CaptureQueue`、`selectionReferences` 及对应数据逻辑。
- 利润与 ERP：`ProfitPanel`、`MonthlyLedger`、`CostMatching`、ERP 扩展、inbox transport 和成本策略。
- UI 与导航：`AppShell`、`UI`、`styles.css`、`WorkspacePortal` 及响应式布局。
- 桌面化：`desktop/`、Electron 外壳、内置浏览器、扩展加载、安装包和桌面 smoke。
- 数据与云端：`database`、Supabase migrations、sync、backup 和权限。跨业务修改前需记录数据契约。

### 项目主线职责

- “项目主线”是唯一的决策与集成中心，负责需求访谈、产品/业务/架构/发布决策、Bug 复现和根因定位、公共 contract 设计、模块拆分、验收标准、合并顺序、提交审查、冲突处理、完整回归、推送、打包和发布。
- 主线默认不实现选品、利润与 ERP、UI 与导航或桌面化模块代码。公共 contract 由主线定义，再指定最相关的专职对话实现；其他专职对话只完成各自适配。
- 主线可直接维护发布元数据、集成状态文档和机械性合并冲突。发布被边界明确的小问题阻塞时，只有取得用户明确授权后，主线才可直接修复专职模块代码。
- 主线完成 Bug 定位后，必须形成可执行任务包：根因、复现方式、影响范围、目标行为、模块边界、验收标准和相关代码基线；随后自动派发给主责专职对话。
- 专职提交审查不通过时退回原专职对话修订；主线不代替专职对话改写实现。修订提交必须重新审查后才能合并。

### 临时子智能体

- 主线可以调用临时子智能体进行只读复现、根因调查、Spec/Standards 审查、contract 对比和测试分析，也可处理边界明确的小型 Bug。
- 小型 Bug 必须同时满足：单一模块、修改文件较少、不改变业务规则、公共 contract、数据库或发布边界，并且已有明确复现信号和有效测试方式。
- 存在可用的主责专职对话时优先派发专职对话。临时子智能体只在快速诊断、专职对话不可用或发布阻塞时承担代码修复。
- 临时子智能体的代码修复必须形成独立 Conventional Commit，由主线审查后合并；不得直接混入未提交的主线修改。
- 普通任务同时最多运行 1 个临时子智能体；跨模块审查或发布最多 2 个。发送最小必要上下文，结果返回后立即结束，不作为长期任务保留。

### 专职对话交付

- 长期专职对话固定为“选品工作台”“利润与 ERP”“全局 UI”“桌面化”；优先复用现有对话，不为一次性任务增加长期对话。
- 专职对话开始实现前必须同步最新 `codex/selection-profit-erp-sync`，只修改自己的模块边界，运行与风险相称的定向测试，并形成独立 Conventional Commit。
- 专职交付必须报告提交号、修改范围、测试结果、风险或遗留问题、公共 contract 变化和主线承接事项。交付后停止执行，等待主线审查或修订任务。

### 模块化基线

- `frontend/src/data/db/clientDatabase.js` 只负责 Dexie 实例、版本迁移和表结构；迁移必须追加版本，不能在业务 repository 中重复定义。
- `frontend/src/data/repositories/selectionRepository.js` 负责选品商品档案、供应商、平台 SKU、采集记录和参考成本；`selectionCatalogUtils.js` 只放无副作用的目录归一化工具。
- `frontend/src/data/repositories/profitRepository.js` 负责月度账本、台账、ERP 正式成本、人工确认和利润定稿；不得把 1688 参考成本静默写入正式利润。
- `frontend/src/data/repositories/workspaceRepository.js` 负责备份、恢复、云种子、诊断和本地数据安全。
- `frontend/src/data/database.js` 是兼容 façade，只用于稳定旧导入路径；新业务逻辑不得继续堆回该文件。
- `frontend/src/styles.css` 是兼容入口，实际样式按 `frontend/src/styles/` 下的页面模块文件维护，并保持入口中的导入顺序。
- 页面之间通过已有 domain contract（台账导入、ERP inbox、同步 envelope、成本策略）传递数据；跨模块新增字段时必须先更新 contract 和对应测试。

## 工作方式

- 先阅读现有实现和 `docs/CODEX_TASK_BOARD.md`，再编辑。
- 每个任务只修改自己的范围；跨范围修改必须在提交说明中列出原因。
- 中大型、跨模块和发布任务使用独立分支与 Worktree；小型、隔离修复可在当前工作区完成。提交信息使用简短的 Conventional Commit 风格。
- 不提交 `.env`、Cookie、Token、ERP 登录信息、业务 Excel、备份和本机数据库。
- 根据影响范围运行验证：业务/共享组件/发布任务至少运行 `pnpm --dir frontend test` 与 `pnpm --dir frontend build`；桌面任务运行 `pnpm --dir desktop verify` 和相关 smoke；小型隔离修复运行最窄的有效测试或视觉检查。
- 最终合并前补充回归说明，并检查中文界面、桌面端和移动端布局。

### 需求确认与执行门槛

- 新业务规则、业务流程、公共 contract、数据库迁移、跨模块任务、权限/安全边界、破坏性操作和发布工作属于需要确认的任务；先使用 `grilling` 解决未决问题，并给出推荐答案。
- 预期行为、影响模块和验收信号都已明确，且属于小型 bug、局部 UI 调整或单模块维护时，可在只读检查后直接执行，不要求重复审批。
- 需要确认的任务先输出一份包含目标、现状、目标状态、范围、非目标、业务规则、交互或数据影响和验收标准的确认摘要，收到用户明确批准后再执行。
- 确认前只进行只读检查、事实查询和方案草拟；不编辑代码或仓库文档，不创建任务，不提交、不推送、不发布，也不修改持久配置。
- 确认后在批准范围内连续完成实现、测试、审查、提交和集成，不为常规步骤重复请求批准；只有出现新的用户决策或实质性扩展范围时才重新确认。
- `grill-with-docs` 和 `domain-modeling` 在访谈期间只准备草稿，确认后才更新 `CONTEXT.md` 或 ADR。
- `to-spec`、`to-tickets` 和 `wayfinder` 默认接入 `docs/CODEX_TASK_BOARD.md` 及现有专职任务，不自动创建 GitHub Issue。
- Shopeers 的人工配置向导使用 PowerShell；通用行为由 `shopeers-development-workflow` Skill 规定。

### 对话自动路由与同步

- 收到用户任务后，先依据“多任务边界”和任务看板判断唯一主责模块。需要确认的任务完成需求确认后，主线自动发送完整任务包，并向用户简短报告目标对话、主责范围、代码基线和验收标准，不再为常规转交重复请求批准。
- 单一模块任务只派发给主责专职对话。涉及多个模块、公共 contract、数据库迁移或共享文件时，主线保留总控，先定义 contract、依赖顺序和共同验收，再向真正受影响的专职对话分别派发；各模块必须形成独立提交。
- 默认路由：商品档案与 1688 参考采集转“选品工作台”；台账、利润、ERP 成本与回传转“利润与 ERP”；React 页面壳、导航和响应式转“全局 UI”；Electron 外壳、内置浏览器、扩展加载、安装与桌面构建转“桌面化”；跨模块决策、数据契约、审查、合并和发布留在“项目主线”。
- 优先复用五条长期项目对话。只有现有边界无法承接且用户明确批准时，才创建新的长期专职对话；一次性调查使用临时子智能体。
- 广播只发送给受影响的对话。目标对话正在执行时，将新任务排队，不并发改写同一模块；关联对话只能记录依赖或提交自己范围内的适配。
- 对话消息只同步意图，不同步代码。专职对话通过独立提交交付，主线通过 `codex/selection-profit-erp-sync` 审查、按依赖顺序合并、执行完整回归并推送 GitHub。
- 每次转接保留简短记录：目标对话、主责范围、受影响对话、代码基线、验收命令和合并前置条件。

## 常用入口

- `/workspace`：经营概览
- `/products`：选品商品库
- `/products?view=reference`：成本与利润参考
- `/products?view=pending`：待确认采集
- `/profit`：月度利润核算
- `/ledger`：月度账本
- `/cost-matching`：ERP 成本核对

### Skill 使用说明

- Skill 是工作建议和领域提示，不是业务事实的最终来源；代码、测试、数据契约和用户已确认的当前需求优先。
- 项目专用 skill 缺失、过时或与当前实现冲突时，不得假定其内容正确；先记录冲突并以代码与测试复核。
- React UI 可使用可用的 UI skill；如果该 skill 未安装，按现有组件、样式模块、浏览器视觉检查和测试执行，不阻塞普通开发。
