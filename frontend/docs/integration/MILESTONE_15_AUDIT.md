# 里程碑 15：商品采集与利润主链路浏览器验收

状态：本机 Beta 的采集入库、ERP 正式成本和选品回流主链路已验收  
审计日期：2026-08-07

## 采集到商品库

使用临时 1688 来源登记完成：

```text
新增采集
→ 保存 1688 来源、商品 ID、供应商编号/名称
→ 编辑商品草稿
→ 补齐平台 SKC
→ 补齐平台 SKU、1688 来源 SKU、采购价
→ 确认写入
→ 正式商品库显示 1 个启用商品、1 个平台 SKU
```

验收结果：

- 待确认采集从 1 条归零。
- 商品库出现 `QA-CAP-SKC-20260807` 和 `QA-CAP-PLATFORM-SKU-20260807`。
- 1688 参考单件成本显示为 `¥13.00 CNY`。
- 成本被标记为选品参考，不会自动覆盖月度正式 ERP 成本。

## 利润主链路

临时销售台账验证：

```text
上传 CSV
→ 自动映射并校验 3 行
→ 写入 2026-07 月度账本
→ 利润面板复制平台 SKC
→ 保存 ERP 请求（工作区 + 账本 + 查询单位）
→ 导入 2 行 ERP 成本
→ 发布正式成本
→ 精确利润 ¥266.50
→ 定稿
→ 选品参考回读 2 个 SKU 的 ERP 历史成本与利润
```

本次发现并修复：

- 利润面板原先只写剪贴板，不保存 ERP 请求。
- 现在利润面板和 ERP 成本核对页共用 `buildLedgerErpCostRequest`，复制平台 SKC 时会持久化请求记录。
- 新增共享函数回归测试，确保工作区、账本、平台 SKC 和默认币种 CNY 均被绑定。

## ERP 缺失时的 1688 兜底

在隔离本地端口 `5175` 的全新 IndexedDB 工作区验证：

```text
导入示例台账
→ 3 个平台 SKU 均缺 ERP 成本
→ 逐项审批 1688 参考成本
→ 面板变为“可定稿”
→ 精确利润 ¥1,546.10
→ 月度定稿
→ 选品参考显示定稿历史
```

每次审批均绑定当前 `2026-07` 账本和平台 SKU，并记录来源说明、审批人和审批原因；ERP 成本仍保留更高优先级。

## 门禁

- Vitest：33 个测试文件、100 项测试通过。
- `pnpm build`：1762 个模块构建成功。
- 移动端 390 × 844 核心页面无页面级横向溢出。
- 核心页面控制台无 error 级别日志。

## 尚未完成

- 尚未连接真实 ERP 登录态或 Chrome 扩展自动执行查询。
- 已完成隔离浏览器工作区的 1688 逐项审批、精确利润和定稿验收；尚未连接真实 ERP 登录态或 Chrome 扩展自动执行查询。
- 生产 PostgreSQL、JWT/RLS、跨设备并发和云端部署仍待用户明确批准后实施。
