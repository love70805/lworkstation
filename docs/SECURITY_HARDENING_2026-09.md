# 2026-09 安全修复与验收

状态：用户已于 2026-09-07 批准修复、测试和集成，随后进行上线验收。起始基线 `108890d7f171d35811d71f208c894fc170050faf`。

## 已确认范围与契约

- 当前任务承接数据与云端、安全契约、集成和 GitHub 防护。本机不存在看板中的专职任务；桌面依赖和 ERP CSV 两项边界明确的维护使用临时子智能体、独立 Worktree 与独立提交。前端依赖升级在 CSV 交付后单独执行。
- 人民币、平台 SKU 唯一性、ERP 正式成本优先、1688 参考成本、历史利润和完整审计存储保持现有口径。修复不得自动修改或重算历史利润。
- 业务动作的投影和授权共用一个显式注册表；所有现有业务动作必须有权限规则。新提交的未注册动作拒绝。已保存的历史审计仍可恢复，不重写其身份或内容哈希。
- `viewer` 不能写业务表。商品合并引起的删除仍属于选品维护，由 admin/selection 执行；服务端同时检查工作区、对象和归属，避免补上角色校验后仍可修改他人的私有对象。工作区设置保持 admin 权限。
- 私有选品内容仅其所有者和现有 admin/operations/finance 可读。恢复的商品、子表和审计快照遵循同一规则；数据库直接读取审计也必须受 RLS 保护。完整历史保留在服务端，对无权成员过滤响应，不删除历史记录。月度经营事实继续按工作区共享。
- 数据库权限修复追加迁移；不改写已执行迁移。数据库连接必须验证可信 CA 和主机名，不允许连接串覆盖严格 TLS 设置。开发 PostgreSQL 只绑定回环地址，密码由环境配置且无固定默认值。
- CSV 只在导出层将外部文本做公式安全处理；原始证据、数量和成本数值不变。更新依赖后保持既有路由、文件导入导出和受控桌面更新行为。
- GitHub 主分支与集成分支要求 PR 和通过 CI，禁止强推/删除；开启 Dependabot 告警和安全更新。仓库代码和数据库策略先通过验收，再操作生产环境。

## 交付顺序

1. 权限注册表、对象权限、私有审计响应、RLS 与负向回归。
2. 前端依赖及文件导入导出；ERP CSV 独立提交；Electron 与构建依赖独立提交。
3. 严格数据库 TLS、开发数据库配置和对应验证。
4. 审查各提交，合入 `codex/selection-profit-erp-sync`，完整回归后推送；配置 GitHub 门槛并核验 CI。

## 验收信号

- viewer、停用成员、跨工作区、伪造 actor 和未注册动作均被拒绝，拒绝后无业务写入；admin/selection 的正常商品合并与合法的 ERP 生命周期继续工作。
- selection/viewer 从恢复 API 或直接审计查询均读不到他人的私有内容，所有者及有权角色可正常读取，历史审计仍完整保存。
- 使用隔离 PostgreSQL 引擎实际执行新增迁移与 RLS 角色矩阵；不得只依靠字符串检查或模拟 SQL。
- 恶意 Origin 读取开发服务被拒绝；危险 CSV 文本不再作为公式；正常中文、引号、换行和真实成本数值保持语义。
- 不可信数据库证书连接失败，可信证书连接成功；Compose 配置只暴露 `127.0.0.1:55432`。
- 前端 `test`、`build`、`release:check`，ERP bridge/inbox，desktop `verify` 与隔离 packaged/update smoke，中文桌面与移动端布局检查；审计残项按实际可达性说明。
- 线上数据库迁移、真实 ERP/1688 登录、正式安装包和发布必须如实记录验收结果。仅有预览包或静态测试不得标记已上线。

## 审查备注

- 仓库 Electron skill 中“自动更新延期”的描述与 beta.7 现有实现不一致；以当前受控更新代码、发布状态和已确认需求为准。
- 本轮使用临时完整 MinGit 进行远程同步；不修改 Codex 内置 Git 或全局凭据配置。

## 完成记录

- 代码修复：显式动作注册与授权、active 成员验证、选品对象归属与跨工作区检查、私有审计 RLS/恢复一致过滤、删除对象的服务端权限元数据及禁止复用 ID、严格数据库 TLS、本机数据库回环绑定与无默认密码。追加 `0010`，不改写历史审计/哈希和月度利润。
- 独立复审补充并关闭了删除后历史重新可见、所有者历史被误过滤、伪造空删除、复用已删除 ID 与 SQL NULL 放行问题。已用真实 PostgreSQL 同时验证同步接口和直接 RLS 路径。
- ERP CSV 提交 `4dcb9be`，桌面依赖提交 `faf1fd9`，前端依赖提交 `473cb71` 已分别审查并合入安全分支；锁文件仅机械性解决 PGlite 新依赖与 esbuild 更新位置冲突。
- 前端与桌面全等级依赖审计均为 **0** 告警（原分别 34 / 38）。SheetJS 固定官方 CDN 0.20.3 及锁文件完整性；Vite 6.4.3、React Router 7.18.3、Happy DOM 20.8.9、Electron 44.2.0、electron-builder 26.15.3。Dependabot 不替代官方 CDN 依赖的人工版本复核。
- 最终代码基线 `c961ca8`：`pnpm --dir frontend release:check` 通过，73 文件 / **510 项**测试、生产构建、ERP bridge/inbox、同步/种子/schema/部署门禁全部通过；`pnpm --dir desktop verify` 通过。实际 PostgreSQL 安全回归 12 项、TLS 回归 4 项包含在完整套件中。
- 默认大量并发曾触发已有 5 秒测试超时；将测试 worker 限为 2 后完整套件通过，未调整断言或超时阈值。
- 真实 Vite 外部 Origin 资源读取/预检无允许来源响应头，本机来源正常；实际 CSV/XLSX worker 导出、解析与校验保留中文、前导零和数值精度。
- 独立空工作区的 `/workspace`、`/products`、`/products?view=reference`、`/profit`、`/ledger`、`/cost-matching` 在 1440×900 与 390×844 下均正常加载，未出现页面异常或整页横向溢出；截图留于本机 `qa/security-ui/`。本轮未使用真实业务数据做页面验收。
- 桌面模块 Windows x64 预览 NSIS 构建、打包 smoke 和更新夹具 smoke 通过，ERP/1688 扩展加载、隔离及 ERP v2 evidence acknowledge 正常。更新 smoke 会截获安装调用，不能代替真实覆盖安装与重启验收。
- 推荐 ERP 扩展包更新为 v8.0.16：38,999 bytes，SHA-256 `CD5D824B61A71DCBE31D780E2BE0031D4564654B5674C7500326D0DECDEFDD53`；已发布 v8.0.15 归档保持不变。
- GitHub 集成、受保护分支 CI 与合并结果待本次 PR 完成后补记。官方桌面版本仍为 0.2.6-beta.7，安全候选未发布。

## 上线验收前置条件

- 未发现本机服务端环境配置、GitHub deployment secrets 或部署记录，因此尚未操作线上数据库、云端 API 或正式发布资产。真实 ERP/1688 登录、目标数据库迁移及证书、真实 NSIS 覆盖安装/重启仍待目标环境确认。
- 数据库必须先迁移再切 API，完整备份包括内部删除权限元数据；具体步骤见 [数据库安全配置](integration/DATABASE_SECURITY_SETUP.md)。
- 本次提交的 `__fixtures__/tls/localhost-test-key.pem` 仅为新生成的公开测试夹具，从未用于真实服务；真实业务密钥、数据库和备份未提交。
