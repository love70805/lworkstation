# 利润冻结报告交付（2026-09-13）

基线 `6e06577` 已包含主线 `3ecc33c`。本专职负责利润、补充来源、报表导出及相关本机数据保护；跨数据文件修改依据已批准的 `FULL_UPDATE_CONTRACT.md`，不新增云 SQL 或同步实体。

## 交付行为

- Dexie 追加 v15 四表：monthlySupplementBatches、monthlySupplementRows、profitReports、profitReportLines。原表原行不重算、不迁移业务值。
- 代发 XLSX/CSV、扣款 XLSX 支持工作表、物理表头、字段映射、本人标记、实际店铺及逐行预览。采用整月完整候选，替换留存旧版本；重复源行、业务冲突和过期预览拒绝。
- 手工总代发必须显式填写，真实零允许；新来源不自动覆盖用户采用件数。扣款保持符号、未取得与明确零分离。
- 未扣款报告原子冻结商品、成本、仓储费率、代发来源和文件。财务对账引用该基础，仅增加独立扣款；重开须有说明并产生新修订，旧文件可按原字节重下载。
- Decimal 80 位字符串用于新公式、SKU/SKC/摘要与保存快照累计，最终金额向零截断两位。商品视图只汇总商品利润；代发只在整月报表加一次。
- XLSX 按批准模板生成计算值、完整文本标识、动态店铺页、斑马纹及独立扣款页。默认样式为无填充，合计浅底连续，左右区域间隙保持空白。
- 完整本机备份含四表及原文件；恢复先检查引用、范围、唯一采用、金额与文件哈希，异常不修改原库。含本机报告时拒绝旧云种子/云恢复替换父账本。本机报告审计不进入旧同步协议。
- 成本页仅局部修正状态行间距和未登记提示；复制 SKC 是查询便捷操作，关联自动登记失败时引导重试。

## 验证

- `pnpm --dir frontend test`：最终 96 文件、612 项通过；原仓储费率/重开弹窗交互两项原样保留，新增错误状态两项放在独立文件。
- `pnpm --dir frontend build`：通过；存在 XLSX 静态/动态导入与大 chunk 提示，不影响构建。
- `pnpm --dir desktop verify`：12 组通过。
- `pnpm --dir frontend release:check`：通过，含前端测试/构建、ERP bridge/inbox、同步服务、种子、schema 与部署合同检查；未部署、打包或安装。
- `frontend/scripts/profit-reports-smoke.cjs`：隔离 Electron profile、合成数据库、loopback 网络；1024×768、1280×800、390×780 无横向溢出；手工 100 件、真实零和负扣款、两阶段实际下载、原文件重下载字节一致、成本状态与工具栏不重叠。
- 报告集成覆盖真实 v14 数据库升级后全部旧表原行不变、缺四表旧备份恢复、八类损坏父子/工作区/月/重复采用拒绝且全部原表不变、文件生成后写入失败回滚、过期预览、月范围、晚到 ERP 不改变基础、修订保留和删除保护。
- 合成来源闭环覆盖跨工作表、跨文件完整候选、重复采用幂等、重复源行/业务单号冲突拒绝、文件追加保留采用 100 件、旧批次与源行留存且仅一个采用版本。
- 活动账本旧罚款或非法 exact 数值不静默回退旧公式：真实组件显示待处理原因与账本入口、禁用定稿；历史锁定快照仍使用原路径。新界面公式与独立扣款说明保持一致。
- 精度测试覆盖 `0.0099999999999999999` 跨 0.01 的摘要、SKC、已保存行还原和最终展示。旧成本测试显式加入测试专用零代发前置；保留原成本证据、权限、人工零与微小正数、撤销和过期断言。旧云恢复测试明确不包含本机冻结报告，完整文件恢复由新集成测试覆盖。
- SheetJS/openpyxl 独立读取验证页签、文本长 ID、格式、对齐、行列尺寸及打印参数。artifact-tool 逐表渲染确认默认底色、连续合计、页布局；其导入会把纯数字文本误作数值，原 XLSX 的完整 inlineStr / `@` 已通过独立读取校验，不能用该渲染器的科学计数显示判定原文件丢 ID。
- 真实补充表只读经过生产解析入口：代发 XLSX 4 页/8681 物理行，CSV 6321 行，扣款 89 页/149497 物理行；仅保存结构计数，不采用或写入真实业务记录。

## 本机证据与承接

原始证据保留在本 worktree `archive/profit-frozen-reports/`，含两份合成 XLSX、九张逐表渲染图、独立读取结果、真实样本结构计数和 `ui/` 三尺寸截图/实际下载。证据哈希见该目录 `hashes.json`，不将业务源文件或本机数据库提交 Git。

主线审查独立提交后集成；桌面专职已承接正式外壳的 CSV 本人筛选、两阶段报告与重开/重启验收。此交付不表示候选已打包或发布。文件保留和四表仅本机语义已明确；云扩展另行遵循公共合同决策。
