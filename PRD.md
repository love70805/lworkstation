# 选品工作台与 ERP Assistant

> **历史文档提示（2026-08-06）：** 本文记录前端原型阶段的产品假设，其中“仅本地、不做云端协作”“月度账本和统一工作区属于 P2”等内容已经失效。当前开发以 [`frontend/docs/integration/PRODUCT_SPEC.md`](frontend/docs/integration/PRODUCT_SPEC.md) 为主规格，以同目录下的基线、兼容契约、领域模型和云端架构文档为约束。

## 前端产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档状态 | Draft 1.0，供功能原型和正式开发使用 |
| 产品定位 | 本地优先、可靠、可核对的电商效率工具 |
| 产品组成 | 选品工作台、ERP Assistant |
| 当前基线 | 选品工作台 `v0.0.1-beta.2`；ERP Assistant 以 `v8.0.0` 为规则和扩展基线 |
| 本文范围 | 前端功能、页面、数据状态、用户流程、交互反馈和验收标准 |
| 本文不包含 | 视觉稿、品牌规范、具体技术实现、后端代码 |

> 视觉、布局、组件和具体交互形式由后续 Stitch 原型确定。本文只约束业务目标、数据、权限、状态、规则和验收结果。

### 文档边界

- 本文中的“页面”“模块”和排列顺序仅用于归类需求，不代表固定路由、布局或组件树。
- Stitch 可以合并或拆分入口、调整信息层级、选择表格/列表/分步流程等表达方式，只要完整覆盖业务能力。
- PRD 不规定导航位置、卡片形式、弹窗/抽屉、按钮位置、颜色、间距、字体、断点或动画。
- UI 方案不得改变数据含义、计算规则、操作权限、状态机和失败处理原则。
- 原型评审关注“任务能否完成、状态能否理解、数据能否追溯”，不以本文件中的模块顺序还原程度为标准。

---

## 1. 产品背景与问题

### 1.1 现状问题

用户目前需要在 1688、ERP、店铺后台和表格之间重复搬运数据：

- 1688 确认订单里的规格、数量、商品小计和运费容易被手工抄错。
- 采集结果如果直接进入正式商品库，缺字段、重复商品和错误规格很难追回。
- ERP 采购成本需要跨分页、订单明细和 SKU 映射读取，部分数据不能用部分成功替代。
- 销售数据、采购成本、仓储费和扣款经常来自不同文件，利润结果必须能解释和复核。
- 本地工具断开、浏览器刷新、存储失败或导入格式变化时，用户需要知道数据有没有写入，而不是看到一个模糊的成功提示。

### 1.2 产品要解决的问题

1. 把外部页面采集和正式数据写入分成两个明确阶段。
2. 让每个数字都能追溯到来源记录、文件或用户修改。
3. 让长任务、失败任务和缺失成本可见、可重试、不可静默跳过。
4. 在不引入云端账户和复杂运维的前提下，提供本地保存、备份、恢复和迁移。
5. 让两个工具可以共享入口和数据合同，但不把不同业务规则混成一个页面或一个计算器。

---

## 2. 产品目标与非目标

### 2.1 目标

- 用户可以从 1688 确认订单采集一件商品，核对后写入商品库。
- 用户可以搜索、筛选、编辑、导出和恢复商品资料。
- 用户可以在 ERP 采购页面执行完整查询，得到仓库 SKU 的历史采购成本。
- 用户可以将销售数据导入利润面板，应用采购成本，查看利润和缺失项。
- 用户可以明确区分草稿、待确认、已完成、失败和已忽略数据。
- 任意失败不会把部分数据伪装成完整成功，也不会静默覆盖正式数据。
- 用户可以使用本地备份在同一台或另一台电脑恢复数据。

### 2.2 非目标

- 不做面向外部客户的 SaaS、多租户、团队权限或在线协作。
- 不自动替用户决定商品是否值得采购、售价是多少或是否上架。
- 不把“数据分析”变成复杂 BI 平台；分析只服务于核对经营结果。
- 不在第一阶段强行合并为一个桌面超级应用。
- 不从 8.1 停止的工作台、Dashboard、Bridge 或 background 代码反向改写 ERP 8.0 基线。
- 不因为界面原型需要而增加未定义的数据字段或计算规则。

---

## 3. 目标用户

### 3.1 主要用户

| 用户 | 使用频率 | 主要任务 | 判断成功的标准 |
| --- | --- | --- | --- |
| 选品/采购人员 | 每日 | 采集确认订单、补齐规格和数量、处理重复商品 | 不用重复录入，确认前能发现异常 |
| 店铺运营/财务 | 每日或每周 | 导入销售数据、匹配成本、核对利润 | 每个利润结果都能追溯，缺失成本可见 |
| 负责人/小团队管理者 | 每周或每月 | 查看商品库、处理异常、导出和备份 | 数据安全，状态清楚，操作不依赖开发者 |

### 3.2 用户假设

- 用户主要使用 Windows 和 Chrome/Edge。
- 用户可以使用 CSV、XLSX、TSV 和浏览器扩展，但不应该需要理解 API、JSON 或数据库。
- 用户希望工具稳定地完成重复工作，不希望看到过多推荐、动画和营销内容。
- 同一时间通常只有一位本机用户；数据安全通过本地存储、备份、确认和诊断保证。

---

## 4. 产品形态与边界

### 4.1 载体决策

产品由两个独立业务工作区组成，可在未来使用一个启动器或统一导航进入，但必须保留：

- 选品采集、商品库和待确认数据边界。
- ERP 采购成本和利润数据边界。
- 各自的状态机、错误码和测试。
- 共享的基础组件、状态语言和数据导入/导出约定。

### 4.2 运行形态

| 能力 | 运行形态 | 说明 |
| --- | --- | --- |
| 1688 采集 | Chrome 扩展/用户脚本 + 本机工作台 | 只读取允许的确认订单页面，发送到本机服务 |
| 选品工作台 | Windows 本地应用或本机 Web UI | 保存商品库、待确认和备份，不依赖云端 |
| ERP 采购成本 | Chrome 扩展页面浮层 | 读取用户最近一次 ERP 查询的完整参数 |
| 利润核算 | 本地浏览器应用 | 导入 CSV/XLSX，使用成本 TSV 或成本结果计算 |
| 备份迁移 | 本地文件 | 用户主动导出和导入，导入前预览并校验 |

### 4.3 关键边界

- ERP 采购成本是利润计算的输入，不在利润页面内重新猜测采购规则。
- 选品商品库是商品资料和采购事实的存储，不等于 ERP 销售账本。
- 统一入口只负责导航和健康状态，不合并两个领域的指标和数据。

---

## 5. 核心业务流程

### 5.1 选品采集到商品库

1. 用户打开 1688 确认订单页面。
2. 扩展检查当前页面类型、连接配置和工作台可用性。
3. 用户点击“发送到工作台”。
4. 扩展读取商品事实，生成 `CaptureEnvelope v1`。
5. 本机服务校验协议、鉴权、请求大小、来源和幂等键。
6. 合法数据进入待确认队列，不直接进入商品库。
7. 用户打开待确认，检查商品、SKU、规格、数量、价格、运费和警告。
8. 用户可以补充、编辑、合并、忽略或确认入库。
9. 确认后写入商品库并生成采集追踪信息。
10. 用户可以从商品库搜索、编辑、导出或进入检查器。

### 5.2 ERP 采购成本核算

1. 用户在 ERP 采购管理页设置并执行真实查询。
2. 扩展捕获本次查询的完整参数，并显示“已捕获最近查询”。
3. 用户点击“开始核算”。
4. 扩展读取采购列表全部分页，并校验总数和唯一订单数。
5. 扩展读取有效采购单的明细，排除作废单。
6. 扩展排除当前自然月、无效日期、无 SKU、非正数量和非正单价。
7. 按仓库 SKU 分组，选择真实 1688 记录或普通采购记录，计算数量加权平均成本。
8. 扩展读取每个仓库 SKU 的平台 SKU 映射。
9. 所有分页、明细和映射成功后，生成结果表及 TSV/CSV。
10. 任一链路失败时终止本次核算，不允许复制或导出部分结果。

### 5.3 销售数据到利润结果

1. 用户进入利润核算面板，选择 CSV 或 XLSX 文件。
2. 系统读取文件、识别编码、解析字段并生成预览。
3. 用户确认店铺、月份、行数、异常和字段映射。
4. 系统按规则分组销售行，排除盘亏，累计销售、销量和扣款。
5. 用户导入或粘贴采购成本结果。
6. 系统按平台 SKU/仓库 SKU 规范化匹配成本，并列出缺失和重复覆盖。
7. 用户修正成本或重新执行 ERP 成本核算。
8. 系统根据固定公式计算采购成本、仓储成本、扣款和利润。
9. 用户查看明细、过滤异常、修改仓储费率或导出结果。
10. 用户可保存草稿或完成本次核算；完成后才允许进入后续月度汇总功能。

---

## 6. 功能域与参考入口

以下入口名称用于需求追踪。Stitch 可根据任务流重新组合，不要求一项功能域对应一个独立页面。

### 6.1 共享页面

| 页面 ID | 页面名称 | 优先级 | 说明 |
| --- | --- | --- | --- |
| SHELL-01 | 工作区入口 | P1 | 两个业务入口、连接和本地数据健康状态；不做营销首页 |
| SHELL-02 | 全局诊断 | P1 | 聚合连接、存储、版本和最近失败任务；可复制诊断信息 |

### 6.2 选品工作台页面

| 页面 ID | 页面名称 | 优先级 | 说明 |
| --- | --- | --- | --- |
| SEL-01 | 商品库 | P0 | 正式商品资料的查询、编辑、导出和详情查看 |
| SEL-02 | 待确认采集 | P0 | 所有外部采集先进入此处，确认后才入库 |
| SEL-03 | 商品编辑/确认 | P0 | 单件商品的字段补充、SKU 校验和入库确认 |
| SEL-04 | 任务检查器 | P1 | 检查缺字段、状态门槛、规格和采购数量问题 |
| SEL-05 | 连接设置 | P0 | 扩展配对、连接码、服务状态和重新连接 |
| SEL-06 | 数据安全 | P0 | 自动备份、手动备份、恢复、存储状态和危险清除 |
| SEL-07 | 采集批次详情 | P1 | 查看请求、来源、警告、重试和处理记录 |

### 6.3 ERP Assistant 页面

| 页面 ID | 页面名称 | 优先级 | 说明 |
| --- | --- | --- | --- |
| ERP-01 | 采购成本浮层 | P0 | 在 ERP 采购页面启动核算、查看过程和结果 |
| ERP-02 | 利润核算面板 | P0 | 导入销售数据、匹配成本、计算和导出利润 |
| ERP-03 | 导入预览 | P0 | 销售文件解析结果、字段映射、异常和确认写入 |
| ERP-04 | 成本匹配区 | P0 | 成本导入、匹配统计、缺失项和覆盖规则；可作为 ERP-02 模块 |
| ERP-05 | 月度账本 | P2 | 保存、继续和查看不同月份的核算结果 |
| ERP-06 | 经营分析 | P2 | 只对已完成账本进行趋势和异常汇总 |
| ERP-07 | 数据管理 | P1 | `.erpa` 或等价迁移包导出、预览、合并、覆盖和清除 |

P0 页面必须先形成可靠闭环；P1/P2 页面不能提前引入未验证的数据存储或计算规则。

---

## 7. 共享功能要求

### 7.1 工作区入口（SHELL-01）

#### 模块

1. 工作区列表：选品工作台、ERP Assistant。
2. 最近使用：最近打开页面、最近任务状态和时间。
3. 健康状态：本机服务、扩展连接、数据存储、备份时间、版本。
4. 快捷入口：打开工作区、检查连接、导入备份、查看失败任务。

#### 需求

- 首次启动不得展示演示商品或虚构指标。
- 如果一个工作区不可用，另一个工作区仍可以打开。
- 健康状态必须区分“正常、未配置、连接中、异常、未知”。
- 点击异常状态可以直接进入对应诊断或设置页面。

### 7.2 全局诊断（SHELL-02）

#### 必须展示

- 应用版本、扩展版本、协议版本。
- 本机服务地址和健康检查结果。
- 最近一次采集、成本核算、导入和导出任务。
- 最近失败的错误代码、时间和可重试动作。
- 本地数据目录和备份状态，但不展示密钥和敏感原始数据。

#### 操作

- 重新检查全部状态。
- 复制脱敏诊断摘要。
- 跳转到连接设置、数据安全或对应任务详情。

---

## 8. 选品工作台功能需求

### 8.1 商品库（SEL-01）

#### 功能与信息要素

1. 页面状态栏：商品总数、待处理提醒、最后同步/备份时间。
2. 搜索区：商品名、平台商品 ID、平台 SKC、SKU、供方货号、来源 URL。
3. 筛选区：店铺、销售状态、是否缺字段、是否有警告、采集时间。
4. 操作区：新增、批量编辑、导入、导出、打开待确认。
5. 商品列表：商品主图、名称、店铺、SKC、SKU 数量、采购成本、状态、更新时间。
6. 详情区：商品基础字段、SKU 明细、采集来源、修改记录、关联批次。

#### 功能要求

- 默认按用户最近使用的排序和筛选展示，首次使用不预设虚构内容。
- 选中多项后才显示批量操作，批量操作必须显示影响数量。
- 列表支持分页或虚拟滚动；切换筛选不清除当前编辑草稿。
- 商品重复识别按请求 ID、来源商品 ID、规范化来源 URL、平台 SKU 顺序执行。
- 缺失值不能被显示为 `0`、空字符串以外的伪值或默认成本。
- 价格排序时缺失价格排在最后，不作为零参与排序。
- “未分配”店铺包含无店铺信息或至少一个空店铺分配的商品。
- 列表中的每个商品都可跳到详情、采集批次和原始来源。

#### 商品行操作

- 查看详情。
- 编辑商品。
- 复制 SKU/SKC/来源 URL。
- 标记销售状态。
- 移入检查器。
- 导出选中商品。
- 删除：二次确认，并说明是否进入可恢复备份。

### 8.2 待确认采集（SEL-02）

#### 功能与信息要素

1. 批次栏：批次 ID、来源平台、采集时间、商品数量、警告数量。
2. 队列筛选：全部、待检查、有警告、重复候选、失败重试。
3. 摘要区：有效商品、字段缺失、SKU 异常、重复候选。
4. 商品核对列表：名称、图像、来源、采购价、运费、数量、SKU 数量、警告。
5. 批量操作：确认全部合格项、忽略、重新解析、删除批次。
6. 单项操作：打开编辑、合并到已有商品、标记为重复、忽略。

#### 功能要求

- 采集数据只能先进入待确认队列，不能直接写正式商品库。
- 队列必须保留原始采集值和用户修改值，便于对比。
- 任一商品有阻断级错误时，不能使用“确认全部”绕过该商品。
- 可以部分确认一个批次，但界面必须显示已确认、待确认和忽略数量。
- 批次失败时保留失败原因和原始请求摘要，支持重试；重试不能重复写入已成功项。
- 关闭页面、刷新或重启后，待确认队列必须可恢复。

### 8.3 商品编辑/确认（SEL-03）

#### 功能与信息要素

1. 商品摘要：名称、主图、来源平台、来源 URL、来源商品 ID。
2. 采购信息：采购价、价格区间、原始价格文本、运费、采购总份数、包装数量。
3. 商品标识：平台 SKC、供方货号、店铺分配、备注。
4. SKU 明细集合：规格、来源 SKU ID、SKU 图片、采购价、采购数量、行小计。
5. 校验面板：缺字段、无效值、重复候选、规格数量冲突、运费依赖项。
6. 操作区：保存草稿、确认入库、合并、忽略、取消。

#### 校验规则

- 商品名称、来源商品 ID、主图和 SKU 列表为核心字段；缺失时显示阻断或警告等级。
- 有运费时必须存在有效采购总份数。
- 包装数量必须大于零。
- SKU 采购数量必须为正数或明确标记为待补充，不得静默按零计算。
- 采购价、运费和小计必须保留原始字段与规范化数值。
- 修改后必须标记“已修改”，确认入库前显示修改摘要。

### 8.4 任务检查器（SEL-04）

#### 检查项

- 商品基础字段缺失。
- SKU 规格为空或重复。
- 采购数量为空、为零或异常。
- 成本/运费依赖字段缺失。
- 店铺未分配或分配不一致。
- 销售状态未达到当前门槛。
- 来源链接、商品 ID 或平台 SKU 不可追溯。

#### 功能

- 支持按问题等级、商品、店铺和规则筛选。
- 每条问题显示规则、当前值、期望值、影响范围和修复入口。
- 修复后可以重新检查单条、当前商品或全部商品。
- 检查器只报告问题，不自动修改商品字段。
- 用户可以标记“已确认但保留”并填写原因；该状态必须可追溯。

### 8.5 连接设置（SEL-05）

#### 功能与信息要素

1. 工作台服务状态：运行、不可达、鉴权失败、版本不兼容。
2. 扩展状态：已安装、未安装、版本不匹配、未配对。
3. 连接码：显示、复制、重新生成、失效提示。
4. 来源限制：允许的平台和页面范围。
5. 测试连接：发送脱敏测试请求，显示完整结果。

#### 功能要求

- 连接码不能以明文写入日志、诊断摘要或导出文件。
- 重新生成连接码前必须说明旧扩展会立即失效。
- 测试连接失败时显示本机服务、浏览器扩展和来源页面三个可能原因。
- 连接状态变化要在商品库和待确认页显示，但不得遮挡当前编辑内容。

### 8.6 数据安全（SEL-06）

#### 功能与信息要素

1. 当前存储：正式数据位置、数据量、最近写入时间。
2. 自动备份：启用状态、最近成功时间、失败原因。
3. 手动导出：导出商品库、待确认、设置和采集追踪。
4. 恢复：选择备份、校验摘要、预览覆盖范围、确认恢复。
5. 危险操作：清空正式数据、清空待确认、重置设置。

#### 功能要求

- 正式写入使用原子替换；写入失败不能损坏旧数据。
- 损坏备份隔离保存，不覆盖最后一份可用备份。
- 恢复前显示商品数量、待确认数量、批次数量和备份时间。
- 恢复后重新读取并校验，失败时回滚到恢复前状态。
- 清空操作必须明确“不可恢复”范围，并要求二次确认。

### 8.7 采集批次详情（SEL-07）

#### 必须展示

- requestId、批次 ID、协议版本、来源、来源 URL（可复制但可脱敏）。
- 采集时间、扩展版本、处理状态。
- 商品数量、有效数量、警告数量、重复数量、失败数量。
- 原始警告代码和用户处理记录。

#### 禁止展示

- 连接密钥。
- 未经用户确认的收货、账号或无关页面信息。
- 不能证明来自源页面的推断值。

---

## 9. ERP Assistant 功能需求

### 9.1 采购成本浮层（ERP-01）

#### 功能与信息要素

1. 查询状态：是否已捕获查询、查询时间、主要筛选条件。
2. 核算入口：开始核算、取消当前任务、刷新查询状态。
3. 进度区：列表读取、明细读取、成本聚合、SKU 映射四个阶段。
4. 结果摘要：仓库 SKU 数量、平台映射数量、有效采购单、明细数、排除项。
5. 结果表：仓库 SKU、平台 SKU、来源单号、核算次数、日期范围、采购量、采购价、单件成本。
6. 明细展开：选中记录、日期、1688/普通采购单号、数量、单价、小计。
7. 输出区：复制成本 TSV、导出 CSV、清空结果、重新核算。
8. 错误区：错误标题、具体阶段、错误详情、重试或重新查询入口。

#### 功能要求

- 扩展只在指定 ERP 采购管理页面运行，不读取其他网站。
- 用户没有先执行查询时不得启动核算，并需要解释原因。
- 进度必须显示当前阶段和已完成/总数，不使用无限旋转代替实际状态。
- 任一分页、明细或平台 SKU 映射请求失败，整次核算失败且不允许复制/导出。
- 结果必须显示排除当月、作废订单、无效数据的数量。
- 结果表支持搜索仓库 SKU、平台 SKU、单号和商品名称。
- 展开明细后显示聚合输入和最终加权成本，便于复核。
- 页面刷新后不自动宣称上一次结果仍然有效；需显示结果时间和查询时间。

### 9.2 利润核算面板（ERP-02）

#### 功能与信息要素

1. 页面状态：当前数据批次、导入时间、店铺/月份、草稿或已完成状态。
2. 导入区：选择 CSV/XLSX、拖放、支持格式说明、最近导入记录。
3. 导入预览入口：行数、店铺、月份、异常和字段映射。
4. 成本区：粘贴 TSV、上传成本文件、应用最新成本、匹配统计。
5. 指标区：销售金额、销量、采购成本、仓储成本、扣款、利润、缺成本数量。
6. 筛选区：店铺、供应商、SKC、平台 SKU、属性、只看缺成本、只看负利润。
7. 明细数据：店铺/SKC、平台 SKU/属性、数量、金额、1688 单号、单件成本、采购成本、仓储/扣款、利润。
8. 行编辑：允许补充单件成本、查看来源、查看匹配记录；修改后标记未保存。
9. 操作区：保存草稿、完成核算、导出 Excel/CSV、清空当前工作区。
10. 警告区：缺成本、异常月份、无效行、扣款行和导入覆盖提示。

#### 功能要求

- 页面首次打开为空白，不自动加载历史明细或旧测试数据。
- 导入前必须预览，确认后才改变当前工作区。
- 同一导入分组再次导入时以新批次替换同组旧数据，并显示新增/更新数量。
- 成本未匹配时可以查看销售明细，但利润结果必须有明显“不可作为最终结果”的状态。
- 用户修改仓储费率后，所有受影响行和汇总同时更新；费率必须持久化并显示更新时间。
- 只读查看模式不得修改成本、费率或原始销售数据。
- 完成核算前必须通过必要字段和成本状态校验；允许用户明确保存为草稿。

### 9.3 导入预览（ERP-03）

#### 预览内容

- 文件名、文件类型、文件大小、解析时间。
- 编码判定：严格 UTF-8，只有 UTF-8 解码失败时回退 GBK。
- 工作表/店铺名、识别月份、总行数、有效行数、忽略行数。
- 字段映射：店铺、SKC、供方货号、平台 SKU、属性、数量、金额、变动类型、订单号、扣款、成本。
- 警告：缺列、重复列、无法解析金额、无 SKC/供方货号、月份混杂、盘亏、扣款行。
- 前 20 条示例和按规则处理后的数量/金额摘要。

#### 操作

- 确认导入。
- 取消导入。
- 重新选择文件。
- 查看字段匹配说明。
- 下载脱敏错误样本（不包含账号、地址和连接码）。

### 9.4 成本匹配区（ERP-04）

#### 功能

- 接受固定 TSV：`平台SKU\t仓库SKU\t1688单号\t单件平均成本`。
- 支持平台 SKU、仓库 SKU 和旧格式单 SKU 回退。
- 匹配前移除 SKU 中全部空白字符。
- 显示总 SKU、匹配数、缺失数、无效成本数、重复覆盖数。
- 同一 SKU 出现多次时，后出现的有效成本获胜。
- 后出现记录单号为空时，不清除先前有效单号。
- 成本为非有限数、零或负数时不得覆盖已有有效成本。
- 匹配结果需要展示来源文件/批次和导入时间。

---

## 10. 后续 ERP 页面需求

以下不是 8.0 基线首批必做，但应在数据合同稳定后设计。

### 10.1 月度账本（ERP-05，P2）

#### 模块

- 月份列表：月份、草稿/完成、店铺数、销售额、销量/订单、总成本、利润/利润率、更新时间。
- 账本详情：只读查看或继续核算。
- 操作：打开、继续、导出、删除。
- 删除前说明只删除该月份，不影响其他月份。

#### 规则

- 一个自然月只能有一个正式账本 ID。
- 草稿可继续编辑，完成账本默认只读。
- 页面不自动加载全部明细，用户打开具体月份才读取。

### 10.2 经营分析（ERP-06，P2）

#### 模块

- 月度销售与利润趋势。
- 选定月份的每日销售趋势。
- 店铺排名。
- 负利润明细。
- 包含/不包含草稿开关。

#### 规则

- 默认只统计已完成账本。
- 图表中的每个数字可以跳转到对应账本和明细。
- 没有已完成账本时显示下一步动作，不显示空图表或估算数据。

### 10.3 数据管理（ERP-07，P1）

#### 模块

- 本地存储摘要：账本数、成本记录数、占用空间、最近备份。
- 导出全部数据：账本、明细、成本记录、设置、版本和校验和。
- 导入迁移包：文件校验、月份预览、数量预览、冲突处理。
- 冲突策略：合并并覆盖同月份、清空当前数据后导入。
- 清除全部数据：明确范围、二次确认、完成后重新检查。

#### 规则

- 迁移包必须有版本、manifest、内容校验和。
- 解包和校验失败时不写入任何现有数据。
- 导入提交使用单事务或等价的原子策略。

---

## 11. 业务规则与计算口径

### 11.1 采购成本规则

对每个仓库 SKU 独立执行：

1. 使用用户最近一次真实 ERP 查询的完整参数。
2. 读取采购列表全部分页，校验接口总数与唯一采购单数一致。
3. 排除作废订单、当前自然月、无效日期、无仓库 SKU、数量不大于零和单价不大于零的记录。
4. 历史有效记录中只要存在真实 1688 单号，就只使用按时间倒序的最近最多 3 条真实 1688 记录。
5. 1688 记录不足 3 条时，不使用普通采购单补足。
6. 完全没有 1688 记录时，才使用最近最多 3 条普通采购记录。
7. 单件平均成本为：

   ```text
   sum(采购数量 * 采购单价) / sum(采购数量)
   ```

8. 单号、数量和价格必须来自同一批选中记录。
9. 对外输出单件成本 4 位小数；计算过程中不先对单条记录金额取整。
10. 任一列表分页、明细或 SKU 映射失败，本次全部失败，不导出部分结果。

### 11.2 利润分组规则

一级分组：

```text
(店铺, SKC；缺失时回退供方货号, 供方货号)
```

二级分组：

```text
(平台 SKU, 属性)
```

- 字段只去除首尾空白，不合并不同供方货号。
- 同一导入组再次导入时，替换同组旧数据，不叠加。
- 文件名去扩展名后作为默认店铺名；用户可以在预览阶段确认。

### 11.3 利润行处理规则

- `变动类型`包含“盘亏”的行完全排除。
- 数量优先读取明确数量字段；为零时回退为“客单发货 + 平台客单”。
- 金额优先读取明确原始金额字段；为零时回退为“客单金额 + 平台金额”。
- 不允许用“数量 * 销售单价”推导销售金额。
- “扣款”“罚款”“违约”行只贡献扣款，不贡献销量、销售金额和销售行数。
- 扣款行优先使用该行非零“客退罚款”，否则使用该行金额字段，统一按绝对值累计。
- 普通销售行另有非零“客退罚款”时，将其绝对值累计到扣款。
- 数量和金额均为零但存在有效扣款的行仍必须保留并计入扣款。

### 11.4 成本匹配规则

- 成本值必须是有限且大于零的数值。
- SKU 匹配前移除所有空白字符。
- 后出现的有效重复记录获胜。
- 空单号不能擦除现有有效单号。
- 无平台 SKU 映射时，仓库 SKU作为平台 SKU 兜底。

### 11.5 利润公式

```text
采购成本 = 销售数量 * 单件平均成本
仓储成本 = 销售数量 * 仓储费率
利润     = 销售金额 - 采购成本 - 仓储成本 - 扣款
```

- 默认仓储费率为每件 `0.7`。
- 汇总总成本只包含采购成本和仓储成本，不包含扣款。
- 汇总总利润包含扣款影响。
- 内部聚合保留完整精度；单件成本合同 4 位小数，金额展示和导出 2 位小数。

### 11.6 选品重复和入库规则

- 重复识别顺序：请求 ID、平台商品 ID、规范化来源 URL、平台 SKU。
- 采集结果先入待确认，确认后才进入商品库。
- 商品列表置顶项优先；价格排序缺失值最后。
- 预览、测试和候选构建不能读取或写入正式数据目录。
- 任何来源字段无法确认时必须保留警告，不生成推断值。

---

## 12. 数据对象与前端展示字段

### 12.1 CaptureEnvelope v1

前端需要能够展示或诊断以下字段，不允许随意改名：

| 字段 | 必填 | 用途 |
| --- | --- | --- |
| schemaVersion | 是 | 协议版本，当前为 1 |
| requestId | 是 | 幂等和批次追踪 |
| source | 是 | 1688、淘宝或天猫 |
| sourceUrl | 是 | 来源页面 |
| capturedAt | 是 | 采集时间 |
| extractorVersion | 否 | 采集器版本 |
| product.name | 是 | 商品名称 |
| product.sourceProductId | 是 | 来源商品 ID |
| product.imageUrl | 是 | 商品主图 |
| product.purchasePrice | 是 | 商品采购价，可为空但必须有状态 |
| product.priceMin/priceMax | 否 | 价格区间 |
| product.rawPrice | 否 | 原始价格文本 |
| product.shippingFee | 否 | 实际运费 |
| product.purchaseQty | 否 | 采购总份数 |
| product.bundleQty | 否 | 包装数量 |
| product.platformSkc | 否 | 平台 SKC |
| product.skus[] | 是 | SKU 明细数组 |
| warnings[] | 是 | 字段、结构和采集警告 |

SKU 至少包含 `spec`、`purchasePrice`、`imageUrl`，可包含来源 SKU ID、采购数量和行小计。

### 12.2 商品记录

前端展示和编辑至少支持：

- 本地商品 ID、状态、创建时间、更新时间。
- 商品名称、主图、平台、来源商品 ID、来源 URL。
- 平台 SKC、供方货号、店铺分配、备注。
- 采购价格、价格区间、运费、采购总份数、包装数量。
- SKU 列表及每个 SKU 的规格、图片、来源 ID、采购价、数量、小计。
- 采集批次、请求 ID、警告、用户修改记录。

### 12.3 采购成本结果

- 核算任务 ID、查询参数摘要、开始/结束时间、状态。
- 仓库 SKU、平台 SKU 列表、产品名称。
- 来源类型、1688/普通采购单号、核算次数、日期范围。
- 总采购量、总采购价、单件平均成本。
- 选中明细列表和排除统计。

### 12.4 利润数据

- 导入批次 ID、文件名、店铺、识别月份、编码、导入时间。
- 店铺、SKC、供方货号、平台 SKU、属性。
- 销售数量、销售金额、订单号、变动类型、扣款。
- 单件成本、1688 单号、采购成本、仓储成本、利润。
- 数据状态：待匹配、草稿、已完成、存在警告、失败。

---

## 13. 状态机与页面状态

### 13.1 连接状态

```text
未检查 -> 检查中 -> 已连接
                     -> 不可达
                     -> 鉴权失败
                     -> 版本不兼容
已连接 -> 连接中断 -> 重试中 -> 已连接 / 不可达
```

每个状态必须显示：状态名称、影响范围、最后检查时间、可执行动作。

### 13.2 采集状态

```text
空闲 -> 发送中 -> 已接收 -> 校验中 -> 待确认
                              -> 校验失败
待确认 -> 编辑中 -> 已确认入库
                  -> 已忽略
                  -> 合并到已有商品
待确认 -> 重试中 -> 待确认 / 失败
```

### 13.3 采购成本任务状态

```text
空闲 -> 校验查询 -> 读取列表 -> 读取明细 -> 聚合成本 -> 读取映射 -> 可输出
       \\-> 失败 / 取消
```

失败状态必须阻止复制和导出；取消状态必须说明是否已产生本地缓存。

### 13.4 文件导入状态

```text
空闲 -> 读取文件 -> 解码 -> 解析 -> 预览 -> 用户确认 -> 写入 -> 已完成
                                      \\-> 取消
任一阶段 -> 失败（保留原工作区）
```

### 13.5 草稿状态

```text
无数据 -> 已加载 -> 有修改 -> 保存中 -> 已保存
                              \\-> 保存失败
已保存 -> 有修改
```

保存失败时不得把任务状态标记为已保存；用户可以重试或导出当前内存数据。

---

## 14. 空状态、加载状态和错误状态

### 14.1 空状态清单

| 场景 | 必须说明 | 主操作 |
| --- | --- | --- |
| 商品库为空 | 尚未建立商品库，不生成示例商品 | 采集或新增 |
| 待确认为空 | 没有待处理批次 | 打开采集连接说明 |
| 搜索无结果 | 当前条件没有匹配项 | 清除筛选 |
| 未连接扩展 | 采集暂不可用 | 检查连接 |
| 利润面板为空 | 需要销售文件 | 导入文件 |
| 成本未匹配 | 利润不能作为最终结果 | 导入或应用成本 |
| 无备份 | 当前没有可恢复版本 | 导出备份 |
| 无完成账本 | 分析数据不足 | 完成月度核算 |

### 14.2 加载状态

- 初始化：读取本地数据、备份和连接状态。
- 数据集合：加载期间保留已有上下文，并明确区分正在读取、空数据和失败。
- 采集/核算：提供阶段、完成数量、当前任务和取消能力；具体表达形式由原型决定。
- 文件导入：显示读取、解析、校验阶段；大文件使用后台 Worker，不冻结页面。
- 导出：显示准备、生成、写入/下载和最终文件名。

### 14.3 错误反馈

错误消息至少包含：

1. 用户能理解的标题。
2. 发生在哪个阶段。
3. 对当前数据的影响。
4. 下一步可执行动作。
5. 可复制的脱敏错误代码。

错误级别：

- `info`：不影响继续操作的信息。
- `warning`：可以继续，但结果需人工确认。
- `blocking`：必须修复或重试，不允许完成当前任务。
- `fatal`：页面或存储不可用，需要恢复或联系维护者。

禁止使用“操作成功”覆盖后端失败、部分成功或持久化失败。

---

## 15. 交互结果与可用性要求

### 15.1 数据集合

- 大量商品、SKU、成本和利润明细必须能够搜索、筛选、定位和批量处理。
- 用户能够查看来源和计算输入，而不只看到最终数字。
- 批量操作必须明确作用对象数量、阻断项数量和执行结果。
- SKU、订单号、来源 URL 等关键标识需要支持完整查看和复制。
- 具体采用表格、列表、分组视图或其他组件，由原型阶段决定。

### 15.2 操作连续性

- 文件选择、搜索、保存、确认和取消等主要任务需要支持无障碍和键盘操作。
- 切换入口、打开详情或处理确认步骤时，不得丢失未保存数据和当前筛选上下文。
- 所有操作需要有可理解的名称、状态和结果；具体控件形式由原型决定。

### 15.3 确认与撤销

- 删除、清空、覆盖导入、重置连接码必须在执行前再次确认影响范围。
- 批量确认前显示商品/行数量和阻断项。
- 可逆操作应提供恢复能力；不可逆操作明确说明不可逆范围。
- 长任务取消不自动删除已有正式数据。

### 15.4 数据来源可见性

- 每个采购成本结果可查询核算时间和来源单号。
- 每个利润成本可查询匹配来源和导入批次。
- 每个商品可查询采集批次和原始来源。
- 用户手工修改的数据必须保留修改标记和时间。

---

## 16. 终端适配范围

- Windows 桌面端是 P0 主要生产环境，必须支持全部功能。
- 移动端不是 P0 的完整生产环境，可只支持查看、搜索、核对、简单确认和错误处理。
- 手机端不要求完成大批量导入、密集数据编辑或完整采购成本核算。
- 不论终端，关键标识、状态和操作不得因空间限制变得不可理解或不可达。
- 具体断点、导航变化、组件替换和移动端页面结构由 Stitch 与实现阶段确定，不在 PRD 中固定。

---

## 17. 安全、隐私与可靠性要求

### 17.1 数据安全

- 默认本地优先，不上传商品、订单、客户或成本原始数据。
- 扩展只匹配明确的 ERP/1688 页面，不读取其他网站。
- 连接码使用最小权限和短期可替换机制，不进入日志和导出文件。
- HTML、CSV、XLSX、来源文本和错误消息必须进行安全渲染，不能直接注入 HTML。
- 导入文件限制大小、类型和行数，超限时阻止写入。

### 17.2 持久化

- 正式数据写入必须原子化。
- 自动备份失败不覆盖最后一份有效备份。
- 损坏数据进入隔离区并显示恢复入口。
- 预览、测试和构建只能使用隔离数据目录。
- 页面刷新和进程重启后，已确认数据、待确认数据和草稿状态必须符合最后一次成功保存。

### 17.3 可观测性

- 错误监控可使用 Sentry，但默认不发送 SKU、订单号、商品名、来源 URL、文件内容或连接码。
- 诊断信息只记录版本、阶段、错误代码、耗时、计数和脱敏 ID。
- 不默认引入行为分析或用户追踪。

### 17.4 外部依赖

- 正式发布页面不得依赖不可控的公共 CDN 才能启动。
- React、解析器、导出库和字体等依赖必须固定版本并随构建产物发布。
- 网络不可用时，纯本地查看、编辑、备份和历史数据恢复仍应可用。

---

## 18. 性能和容量目标

以下是验收目标，不是允许牺牲正确性的硬性优化要求：

- 工作台启动后在 3 秒内显示可操作的本地状态或明确失败。
- 采集发送成功后 2 秒内显示已接收或明确失败。
- 1,000 个商品、每个商品 20 个 SKU 时，搜索、筛选和打开详情不应冻结页面。
- 大型 CSV/XLSX 解析不得阻塞主界面，显示进度或可取消。
- ERP 成本核算过程中允许并发读取，但结果提交必须原子化。
- 导出文件必须包含生成时间、数据范围和版本信息，便于复核。

---

## 19. 功能验收标准

### P0 主流程

- [ ] 新用户可以完成扩展连接、1688 采集、待确认、编辑和入库。
- [ ] 采集失败、字段缺失、重复候选和服务不可用均有明确状态。
- [ ] 商品库支持搜索、筛选、详情、编辑、导出和恢复。
- [ ] ERP 采购查询成功后，能得到完整 SKU 成本并复制/导出。
- [ ] ERP 任一分页、明细或映射失败时，不产生可误用的部分成本结果。
- [ ] 利润面板支持 CSV/XLSX 导入预览、字段映射、确认写入和取消。
- [ ] 成本匹配显示匹配、缺失、无效和覆盖数量。
- [ ] 利润公式、扣款、盘亏、仓储费率和金额来源符合本文规则。
- [ ] 缺少成本时不能把利润结果标记为最终完成。

### P1 可靠性

- [ ] 自动备份、手动导出、恢复和损坏隔离可用。
- [ ] 刷新、重启和存储写入失败不会静默丢失已保存数据。
- [ ] 所有危险操作有范围明确的二次确认。
- [ ] 所有长任务可以查看阶段、进度和取消结果。
- [ ] 诊断摘要不包含连接码、原始文件和业务敏感字段。

### P2 延展

- [ ] 月度账本可以保存、继续、只读查看和删除单个月份。
- [ ] 经营分析默认只统计已完成账本，并能回到原始明细。
- [ ] `.erpa` 或等价迁移包可以在空数据库导入并校验。

### 终端适配验收

- [ ] 桌面端可以完成全部 P0 任务。
- [ ] 原型阶段明确移动端覆盖范围和不支持的任务。
- [ ] 支持的终端中不存在关键数据不可读或关键操作不可达的问题。
- [ ] 空、加载、失败、成功、只读和未保存状态均有原型或可复现测试。

---

## 20. Stitch 原型交付要求

Stitch 负责决定信息组织、入口数量、页面层级、组件和交互表达。本文中的页面 ID 仅用于检查功能覆盖，不要求逐页照搬。

### 功能覆盖清单

原型需要覆盖：工作区健康状态；商品库；待确认采集；商品编辑和校验；连接与数据安全；ERP 采购成本任务；利润数据导入、成本匹配和核算；以及相关的空、加载、失败、只读、未保存状态。

月度账本和经营分析属于 P2，可以只提供概念流程，不需要在首轮原型中展开全部细节。

### 业务限制

- 不添加本文未定义的业务字段和计算结果。
- 不把待确认数据表达成已经入库。
- 不把成本缺失表达成成本为零。
- 不把失败或部分完成任务表达成完整成功且可导出。
- 每项关键任务都需要覆盖开始、进行中、成功、失败和不可执行的结果，但不限定使用哪种控件。
- 演示数据必须明确是示例，不能与真实本地数据状态混淆。

---

## 21. 开发优先级与阶段计划

### P0：可靠主链路

1. 共享状态模型和错误反馈组件。
2. 选品连接、待确认、商品库和商品编辑。
3. 选品采集批次追踪和本地备份。
4. ERP 采购成本浮层和完整性校验。
5. 利润文件导入预览、成本匹配、公式计算和导出。
6. 所有核心空状态、加载状态、失败状态和窄屏关键流程。

### P1：可维护和可恢复

1. 任务检查器和批次详情。
2. 迁移包导入/导出和冲突处理。
3. 纯本地依赖构建、错误监控和诊断摘要。
4. 大数据量表格、后台解析和性能优化。

### P2：统一工作区增强

1. 月度账本。
2. 经营分析。
3. 统一启动外壳。
4. 更完整的跨电脑迁移和版本升级向导。

阶段之间的前提：P0 的数据合同、计算规则和回归测试通过后，才允许进入 P1；P1 的本地恢复和依赖稳定后，才考虑 P2 的统一载体。

---

## 22. 待确认问题

以下问题不影响 P0 原型，但在实现前需要最终确认：

1. 统一入口是否必须存在，还是两个应用继续分别启动？
2. 商品库是否需要多店铺分配的历史记录，还是只保留当前分配？
3. 利润核算的订单数是否在 P0 直接展示，还是只在月度账本阶段加入？
4. 采购成本结果是否只通过 TSV 传递，还是允许 P1 增加本地成本收件箱？
5. 备份文件是否需要加密，还是先依赖本机文件权限和用户主动保管？
6. Sentry 是否只在用户主动开启诊断后发送，还是允许发送完全脱敏的崩溃事件？
7. 移动端是否只提供查看和简单确认，还是需要支持完整文件导入？

在这些问题确定前，原型可以使用明确的占位状态，但不得自行发明业务规则。
