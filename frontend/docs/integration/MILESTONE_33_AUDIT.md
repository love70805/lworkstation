# 里程碑 33：真实台账浏览器验收与运行态收尾

日期：2026-08-08  
状态：**已完成本机真实数据验收**

## 本次验收

使用真实文件 `680店.xlsx` 在本机浏览器完成完整导入链路：

```text
上传文件
  -> 自动识别“台账变动明细”
  -> 默认勾选平台客单发货、客单发货
  -> 多选供方货号
  -> 展开/收起自动映射
  -> 校验
  -> 写入 2026-07 月度账本
  -> 进入利润核算面板
```

## 结果

- 解析来源：2,198 行。
- 自动识别 4 个供方货号。
- 默认选中 2 种销售变动类型。
- 盘亏 5 行可单独勾选，并保持异常样式。
- 全选供方货号后校验：2,193 行有效、0 行错误。
- 写入后利润面板展示 64 个 SKC、221 个平台 SKU、2,193 件销量。
- 没有 ERP 正式成本时，221 个 SKU 保持“待补成本”，定稿按钮保持禁用。
- 仓储费按人民币每件 0.70 元计入，1688/台账参考成本未自动替代 ERP 成本。
- 利润面板的供方货号筛选跨页面返回后仍保留。
- 桌面端和 390×844 移动端无横向溢出，浏览器控制台无错误或警告。

## 运行态修复

- ERP 成本文件输入增加中文可访问名称，避免浏览器显示默认英文 `Choose File`。
- 复制平台 SKC 增加剪贴板 API 失败时的文本域降级方案。

## 自动验收

```text
45 个测试文件，163 项测试通过
Vite 生产构建通过
本地发布候选验收通过
部署门禁通过
```

## 尚未具备的外部条件

真实 Supabase/Vercel 生产资源、生产同步 API、真实 ERP 登录态和正式成员账号仍需外部账号与权限配置；当前不能宣称已经完成云端生产上线。
