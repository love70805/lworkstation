# 一次完整更新进度

## 基线与授权

2026-09-13：用户建立完整更新目标并要求持续推进，统一候选验收后发布一个 GitHub 版本，本机不安装。详细需求见 NEXT_UPDATE.md。桌面独立范围已派发；本机 v15 四表与两阶段报告保存方案已获用户明确确认，授权实施；尚未完成迁移或功能交付。

- 业务基线 `1932d2d`；范围与桌面合同提交 `f25a523`。
- 主线准备：`.worktrees/full-update-integration`，分支 `codex/full-update-integration`。
- 远端已读取，集成没有遗漏远端新增提交；GitHub Latest 回读为 `v0.2.11`。
- 独立 `Infinite Canvas/` 项目为现有未跟踪目录，不属于本轮，不添加、不修改。

## 任务路由

| 任务 | 范围 | 状态与前置 |
| --- | --- | --- |
| 桌面化 `01a08103-9373-70e0-89dc-746707e8bd40` | 启动、托盘、退出、主题、首页及实际ERP操作链验收 | 桌面和首页包已承接；追加P1-P3隔离Electron全链路验收，仅测试/证据，不单独发布 |
| 利润与 ERP `01a07ff8-148a-7bc3-b8ee-2b4458a60878` | 分析/UI、本机v15、代发扣款与报告 | 78a1f1d/70754a8已承接为f33a390/ee33c8c；591测试/build与独立窗口验收通过；继续独立v15/报告包 |
| 首页与导航（桌面化顺序承接） | 保留经营概览、首页账本、成本内容组合 | 48ecd1b/e8cf485已承接8c07dec/a957992；主线独立明暗三尺寸及真实操作通过；待最终报告包集成候选 |

## 已完成基线验证

在主仓库 `f25a523`（仅文档变更，业务同 `1932d2d`）运行：

- `pnpm --dir frontend test`：86 文件、582 项通过。
- `pnpm --dir frontend build`：通过；存在既有大 chunk 提示。
- `pnpm --dir desktop verify`：11 组通过。
- `pnpm --dir frontend erp:inbox:test`：并发、Windows 锁恢复、原子失败及 HTTP 队列通过。
- `pnpm --dir frontend erp:bridge:test`：桥接契约通过。

这些是改动前基线，不代表新版本已通过验收，也不等于真实 ERP 现场问题已验证恢复。新功能完成后须重新执行完整门禁及真实隔离候选检查。

## 必须保持的交付边界

- 表格示例和业务原表仅本机只读参考，不提交 Git。
- 迁移与两阶段保存已落为 FULL_UPDATE_CONTRACT.md 实施合同；仅隔离开发/测试，不操作真实业务数据库、不连接云端。
- 各模块独立提交，主线审查通过后承接。官方候选最终从集成主 Worktree 构建。
- 下一版全部必需项逐项验收，公开资产上传并回读后才标记发布。当前未构建新候选、未发布、未安装。

## 用户确认记录

用户明确：9月初核算8月利润，扣款9月中收到，即核算当月中旬，仍属于8月账本；同时确认后补扣款沿用原明细、主动更正保留旧报告及本机v15四表、备份恢复测试。此记录是授权，不代表完整更新已交付。

## 桌面体验主线审查与承接

- 专职 `08b7e06e0b89d37385049919c344dce71f28b5d2` 已完成文件边界、IPC主框架校验、退出/启动竞态及主题重入审查，主线承接 `9174805`。
- 主线在准备 Worktree 上执行前端87文件585测试、生产构建、desktop全部12组verify通过；同值主题标记残留经主线独立复现、退回修订并复验通过。
- 原生体验与主题证据保存在主仓库 `archive/desktop-experience-2026-09-13/`，13个PNG/result文件逐项SHA-256回读一致。主线查看三尺寸主题与启动异常样图，确认记录包含实际背景收件/托盘恢复/第二实例/超时重试等检查。
- 边界见 `desktop/DESKTOP_EXPERIENCE_VALIDATION.md`：会话结束为事件模拟，未关闭Windows；更新拦截安装器但执行退出；未作旧版同机性能对照，不宣称固定60fps。完整版本仍须集成后的packaged/update smoke和全部业务验收。

## 利润分析与账本隔离承接

- 专职78a1f1d提供台账添加日期/单价/活动字段、精确每日分析、可展开全部SKC/SKU及页内成本核对，主线承接f33a390；70754a8修复账本列表工作区隔离，主线承接ee33c8c。数据库仍v14，不代表v15报告已交付。
- 主线组合桌面包执行89文件591测试、frontend build、ERP inbox/bridge测试均通过。另使用准备Worktree的5197端口及隔离Electron profile独立运行profit-analytics-smoke：1024×768、1280×800、390×780单壳/无body横向溢出、人工零值与撤销、月份保留视图、快速换店/刷新、旧成本页和外工作区拒绝通过。
- 专职11项合成截图/result已逐一SHA256回读归档，主线独立11项结果保存于archive/profit-analytics-ui-2026-09-13/main-integration；专职只读680样本报告5559源行、5543销售行、0错误、日期完整、419SKU，月精确金额与旧摘要一致。原业务数据不提交。
- 原生脚本首次直接启动已生成ok结果；后续重复的Start-Process等待式启动被自动审批拒绝（仅返回策略阻止），未继续重试。此记录不影响首次已完成且回读的验收证据。
- CostMatching的ledgerIdentityRef effect清理在初次加载/StrictMode可能将当前id设空，专职2771adc修复承接ea65b68，主线复审及StrictMode/初载/切换/卸载定向测试通过，此项关闭。当前浏览器证据没有真实ERP账户，不冒充用户现场修复闭环。

## 首页导航与共享组件复审

- 48ecd1b承接8c07dec：保留经营概览统计/动态/待办/商品成本观察/健康/快捷操作，首页月份与店选择、完整账本入口、共同日分析、4项侧栏及旧成本链接适配。无业务repository/Worker越界。
- e8cf485承接a957992：工作区缺成本合计明确标工作区，不误写为入口月份；主线指出快速月+店smoke重复设置店铺会掩盖丢失，修订后首次组合直接断言URL/选择框，无补设通过。
- 9e332dd承接78efc88：未知销售月合计显示待查，真实零保留数字，组件渲染测试通过。
- 组合运行91文件594测试、frontend build、12组desktop verify通过。随后仅全局合计文案/测试修订，主线在a957992实际运行workspace-ledger-smoke通过：空不造账本、6个月2店、日异常、真零/未知、light/dark三尺寸、旧成本/全账本/快速组合/月店刷新及外工作区排除。
- 主线27项合成PNG/result回读及哈希保存在archive/workspace-ledger-2026-09-13/main-integration（原临时DtKOTL，5197端口）；目视复核1280暗色首页及390未知值，层级、状态和作用范围符合合同。最终候选仍需全模块回归，不据此提前发布。

## 报表包在制审查与ERP验收补充

- 报表专职继续v15、补充来源、冻结报告、原文件留存、备份恢复及界面，尚未交付独立提交。主线不把在制代码当作已实现或已通过。
- 在制审查退回：保留来源时直接引用current.sources后push会污染当前预览对象，需复制并验证重复预览/取消；用户已采用总代发数不能因保留明细而静默回退到明细合计，须保留人工意图或明确本次整月数。
- 主线用当前XLSX库独立复现非A1有效范围B5:C6：header:1解析返回两行，若不保留范围起始行会将真实第6行记录为第2行。已要求补充源表解析保留真实行偏移并测试，UI表头选择也须对应原表。
- 既有packaged smoke由main直接注册请求、VM调用扩展bridge，不能单独证明用户操作链。已向桌面化派发30836d0基线的独立测试任务：真实React自动SKC登记、受控IPC/inbox、非空手动草稿仍收件、载入并发布成本、完整证据接收后关闭采购上下文、复制解析及scope隔离；仅合成ERP，不使用真实账号。最终候选仍需重跑，发现业务bug退回利润专职。
- 精度边界独立复现：实际在制legacyReportLine将profitExact=0.0099999999999999999转换为Number 0.01，若页面继续汇总profit数字则显示0.01，报告精确截断为0.00。已退回利润专职，要求新公式页面/SKC/快照出口优先exact字符串，旧历史公式保持；交付必须用跨两位边界测试关闭。
- 最后用户样本的样式元数据与源文件SHA256已只读抽取至archive/full-update-report-review-2026-09-13/approved-template-style.json，不含业务单元格值。扣款文本仍深色，仅金额及合计红色；已同步专职。
- 2026-09-13对当前集成依赖执行frontend及desktop的pnpm audit --audit-level=low，均返回No known vulnerabilities found；若后续依赖变化需重新验证。GitHub只读回读仍为v0.2.11 Latest，未提前创建新Release。
