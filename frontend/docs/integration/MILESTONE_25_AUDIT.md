# 里程碑 25：工作区首页入口去重

状态：首页关键跳转已完成去重并通过浏览器回归；业务数据、成本规则和账本状态机未改变
审计日期：2026-08-08

## 本次完成

- 修复工作区首页在“账本缺少正式成本”状态下的重复入口：
  - 黄色待办条继续进入 `/cost-matching`，处理阻断问题；
  - 快捷操作第一项改为进入当前账本 `/profit`，查看现有利润核算状态。
- 对“利润复核待办”状态提供反向互补入口：待办进入利润面板，快捷操作进入 ERP 成本证据。
- 无上下文待办时，快捷操作回到销售台账导入，不再伪造业务动作。
- 将入口决策抽取到 `src/lib/workspaceActions.js`，补充单元测试，防止后续回归。

## 浏览器验收

验证流：工作区首页 -> 查看当前待办 -> 点击快捷操作第一项 -> 进入对应的互补页面。

- 首页正常渲染，标题为“经营概览”；
- 当前待办路径为 `/cost-matching?ledger=LEDGER-workspace-default-2026-08`；
- 快捷操作第一项文本为“查看利润核算”，点击后到达 `/profit?ledger=LEDGER-workspace-default-2026-08`；
- 利润面板正常渲染，显示“利润核算面板”和当前账本状态；
- 浏览器控制台无 error/warn。

## 自动化验收

- `pnpm test`：40 个测试文件、138 项通过；
- `pnpm build`：1765 个模块构建成功；
- `pnpm sync:check`：通过；
- `pnpm seed:check`：通过；
- `pnpm schema:check`：通过；
- `pnpm deploy:check`：通过。

当前阶段仍为：**本机真实数据 Beta，核心业务闭环与部署基线已具备；真实云端生产接入仍需批准和环境演练。**
