# Lworkstation 项目开发规则

Lworkstation 是组内使用的中文经营管理工作台，整合选品商品档案、1688 参考采集、ERP 成本回传和月度利润核算。当前交付以纯本机 Windows 桌面版为准，不假定存在云端数据库。

本规则按任务风险制定，不绑定模型版本。用户明确授权及后续修正优先；项目规则不能覆盖上层系统指令或工具实际权限。

## 不可改变的业务口径

- 默认币种为人民币（CNY）。
- 平台 SKC 是商品父级标识；平台 SKU 是工作区全局唯一标识，也是 SKC 下的属性分支。
- 选品工作台负责商品档案、供应商、1688 链接、图片、售价、销售状态和参考成本/利润。
- 利润面板负责月度精确核算：台账收入、ERP 正式成本、仓储成本和最终利润。
- 默认使用 ERP 正式成本；显式人工更正优先级最高，1688 成本只作参考，旧参考审批不能自动升级为正式成本。
- 人工更正只需非负单件成本和更正说明，允许真实零值与微小正数；限定当前工作区、月份、店铺和平台 SKU，自动记录审计，可撤销恢复最新可用 ERP 成本或缺失状态。后续 ERP 回传不能覆盖人工有效值，不能静默覆盖已定稿月度利润。

## 主线与协作

- 项目主线负责端到端交付，可直接完成相关模块的定位、实现、测试、审查、集成、打包和发布；不因跨模块或小修强制转交。
- 先阅读相关实现和 `docs/CODEX_TASK_BOARD.md`，确认基线、在制修改和已批准范围。旧看板中的强制路由或审批流程作为历史记录；当前协作方式以本文件及最新用户授权为准。
- 业务、数据、UI 和桌面模块保持职责分离。必要跨模块变更先简短记录契约、影响范围与验收信号，再实施，不把模块边界变成人工审批边界。
- 仅在独立并行、调查或交叉审查明显有益时委派专职任务或临时子智能体。简单修复直接完成；禁止多个任务同时改写同一文件。
- 委派时说明目标、代码基线、文件范围和验收方式；交付报告提交号、验证结果、契约变化和遗留问题即可。主线可直接修订交付，但必须检查差异并验证受影响行为。
- 已有专职任务按需复用：选品工作台、利润与 ERP、全局 UI、桌面化。仅在用户明确要求时新增长期任务；不为一次性修复创建长期任务。只同步给受影响的任务，进行中的同范围任务先协调或排队。
- 跨工作区协作通过提交交付代码，以最新 `codex/selection-profit-erp-sync` 为集成基线，按依赖顺序审查和合并。临时子智能体使用最小必要上下文，完成后不保留为长期任务。

## 授权与执行

- 用户提出明确修改目标后，在该范围内连续完成实现、测试、审查、提交和集成。常规技术选择、必要跨模块适配及相关小修由主线决定，不重复请求批准。
- 需要新业务决策、实质性扩大范围，或已有要求相互冲突时，先说明问题、推荐方案及影响，只确认尚未授权的部分；其他独立工作继续推进。
- 用户已于 2026-09-14 明确授权“以后完工之后自动发布”：后续获准实施的软件改动完成后，默认连续完成审查、集成、CI、打包、候选验收、GitHub 稳定版发布和公开资产回读，不再另行询问发布确认。同一批批准范围完成并通过验收后统一发布，不为中间提交逐个发版；检查失败或候选未通过时先修复，不发布未验收版本。
- 用户后续明确要求仅准备、预览、候选、Beta 或暂不发布时，以该次要求为准。方案讨论、纯文档和工作规则调整不创建空的软件版本。本机安装依用户授权执行，“本机暂不安装”等约定持续有效。
- 真实数据批量改写或删除、破坏性迁移、已定稿报告变更、账号／凭据／权限变更、付费操作及对外沟通须有明确授权；已有明确授权不再重复询问。
- 已批准功能需要的兼容性迁移文件可以编写并使用隔离测试数据验证。执行到真实数据库前，明确备份、影响及回退方案并取得授权；迁移追加版本，不改旧迁移或静默覆盖定稿数据。
- 不更改用户未要求调整的配置，不提交 `.env`、Cookie、Token、ERP 登录信息、业务 Excel、备份或本机数据库。
- 上述要求不依赖固定访谈技能。需要询问时用简明问题和推荐答案；不要将缺失技能或惯例当作额外审批条件。

## 模块化基线

- `frontend/src/data/db/clientDatabase.js` 只负责 Dexie 实例、版本迁移和表结构；迁移必须追加版本，不能在业务 repository 中重复定义。
- `frontend/src/data/repositories/selectionRepository.js` 负责选品商品档案、供应商、平台 SKU、采集记录和参考成本；`selectionCatalogUtils.js` 只放无副作用的目录归一化工具。
- `frontend/src/data/repositories/profitRepository.js` 负责月度账本、台账、ERP 正式成本、人工确认和利润定稿；不得把 1688 参考成本静默写入正式利润。
- `frontend/src/data/repositories/workspaceRepository.js` 负责备份、恢复、云种子、诊断和本地数据安全。
- `frontend/src/data/database.js` 是兼容 façade，只用于稳定旧导入路径；新业务逻辑不得继续堆回该文件。
- `frontend/src/styles.css` 是兼容入口，实际样式按 `frontend/src/styles/` 下的页面模块文件维护，并保持入口中的导入顺序。
- 页面之间通过已有 domain contract（台账导入、ERP inbox、同步 envelope、成本策略）传递数据；跨模块新增字段时必须先更新 contract 和对应测试。

## 验证与交付

- 按风险验证：小型隔离修复先运行最窄有效测试或视觉检查；影响计算、持久化、共享组件或跨模块行为时扩大回归范围。
- 发布前完成 `pnpm --dir frontend test`、`pnpm --dir frontend build`，以及适用的 `pnpm --dir desktop verify`、桌面 smoke 和实际用户路径验收。UI 改动检查中文、窗口化布局、相关浅深色及窄屏适配。
- 同一候选基线已通过的检查不机械重复；新代码、失败或未解决风险需要补验。纯文档／格式修改检查差异、链接和格式，无需重跑无关业务测试；GitHub 强制 CI 仍须通过。
- 提交与报告包含具体改动、验证结果及实际限制，避免重复过程记录。相关小修可随当前任务形成简短 Conventional Commit，大改按可审查单元拆分。
- 中大型或发布任务使用独立分支与 Worktree；小型隔离修改可在当前工作区完成，保留用户未提交内容。分支默认使用 `codex/`，遵守远端分支保护，通过所需 PR 集成，不强推绕过检查。
- 临时 Worktree 优先使用应用管理目录；手动创建时放在 `.worktrees/<任务>/`，不新增桌面同级临时目录。
- 本任务生成的可重建临时文件可以自动清理：先核对绝对路径、未提交内容、提交是否保留，归档不可重建产物与验收证据；Worktree 使用 `git worktree remove`。不清理其他任务工作区、用户文件、真实数据或唯一备份。
- 本机 QA 候选登记在 `releases/README.md`，安装包放在 `releases/candidates/<版本与用途>/`，原始验收证据放在 `archive/<任务>/`。
- 候选验收、代码合并和公开发布分别记录。发布核对代码基线、标签、安装包哈希、稳定／Beta 通道及公开资产；只有上传并回读公开资产后才标记已发布。

## 常用入口

- `/workspace`：经营概览
- `/products`：选品商品库
- `/products?view=reference`：成本与利润参考
- `/products?view=pending`：待确认采集
- `/profit`：月度利润核算
- `/ledger`：月度账本
- `/cost-matching`：ERP 成本核对

## Skill 使用

- Skill 提供工作指导，业务事实以代码、测试、数据契约和用户确认的当前需求为准；技能的使用仍遵守上层指令。
- 项目专用 Skill 缺失、过时或与当前实现冲突时，说明实际影响并以代码和测试复核；有可用替代方法时继续，不阻塞普通开发。
- React UI 按现有组件、模块样式、视觉检查和测试实施，不强制依赖未安装的 UI Skill。
- 需求、契约和进度优先维护在 `docs/CODEX_TASK_BOARD.md` 及对应集成文档，不自动创建 GitHub Issue。人工配置向导使用 PowerShell。
