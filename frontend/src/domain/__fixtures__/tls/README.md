These certificate/key files were generated solely for isolated localhost TLS
tests. They have never authenticated a service and must never be used outside
tests. The test verifies untrusted CA rejection, explicit trust, and hostname
validation. The deliberately public key fixture is not a production secret.
